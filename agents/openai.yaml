interface:
  display_name: "Orchestrate"
  short_description: "Route substantial work across specialized sub-agents"
  default_prompt: "Use $orchestrate to evaluate whether this task should be split among Sol, Terra, or Luna sub-agents, and coordinate only the useful independent work."
